- name: Demo on loops
  hosts: localhost
  tasks:
    - name: Demo on list values, Each value is string
      ansible.builtin.debug:
        msg: Hello, {{item}}
      loop:
        - Teja
        - Siri
