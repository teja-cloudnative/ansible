- name: Copy MongoDB Repo
  ansible.builtin.copy:
    src: mongo.repo
    dest: /etc/yum.repos.d/mongo.repo

- name: Install the Mongodb
  ansible.builtin.yum:
    name: mongodb-org
    state: installed
